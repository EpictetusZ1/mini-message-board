extends layout

block content
    div
        h3 Send Your Message!
        form(method="POST" action="/new" class="msgForm" autocomplete="off")
            input(type="text" name="usrMsg" placeholder="Add your message")
            br
            input(type="text" name="usrName" placeholder="Add your name")
            br
            button(type="submit") Send Message
